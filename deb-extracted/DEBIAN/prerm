#!/bin/bash
set -e

# Stop the system service before removal/upgrade (best-effort).
if [[ -d /run/systemd/system ]] && command -v systemctl &>/dev/null; then
    systemctl stop lorawan-tracker.service 2>/dev/null || true
fi

exit 0
